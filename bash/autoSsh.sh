# Load SHH key
# This will prompt the terminal for a password
# Can be skipped by not entering a password
# if [ ! -n "$NO_DEFAULT_SSH" ]; then
    # shopt -s globstar
    # eval `ssh-agent -s`
    # ssh-add
# fi
